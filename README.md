🔮 Customer Churn Prediction

A **Streamlit web app** that predicts customer churn using a **Random Forest Classifier**.

🚀 Features
- Upload your own dataset (`.csv`/`.xlsx`) or use sample data.
- Handles class imbalance with **SMOTE**.
- Scales data with **StandardScaler**.
- Shows model accuracy, classification report & cross-validation score.
- Interactive form to predict churn probability for new customers.

🛠️ Tech
- Python, Streamlit, Pandas, NumPy
- Scikit-learn, Imbalanced-learn

▶️ Run
```bash
pip install -r requirements.txt
streamlit run app.py
```
